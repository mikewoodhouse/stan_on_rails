%h1 Players#index
%table
  %thead
    %th Name
    %th From
    %th To
  %tbody
    - for player in @players
      %tr
        %td= link_to player.name, "player/#{player.code}"
        %td= player.seasons[:from]
        %td= player.seasons[:to]
