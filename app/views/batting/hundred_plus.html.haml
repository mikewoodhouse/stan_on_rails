%h2 Centuries
%table
    %thead
        %tr
            %th Runs
            %th Player
            %th Date
            %th Opponents
    %tbody
        - @hundreds.each do |h|
            %tr{class: cycle('odd', 'even')}
                %td= h.runs
                %td= player_name(h.player)
                %td= h.date
                %td= h.opponents
